{
  "name": "@synapse/core",
  "version": "1.0.0",
  "private": true,
  "description": "Domain models, learning engine, curriculum content, and entitlement logic for Synapse. Platform-agnostic — no React, no native APIs.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
